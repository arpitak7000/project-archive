# Project Archive

## Overview
A blank Replit project (imported from GitHub). Currently serves a static placeholder page via Python's built-in HTTP server.

## Stack
- **Frontend:** Static HTML (`index.html`)
- **Server (dev):** Python `http.server` on port 5000

## Workflow
- **Start application:** `python3 -m http.server 5000 --bind 0.0.0.0`
  - Port: 5000
  - Output type: webview

## Deployment
- Target: static
- Public directory: `.` (project root)

## Notes
- This is a starter/archive project with no application code yet.
- Replace `index.html` and the workflow command with your actual application stack.
